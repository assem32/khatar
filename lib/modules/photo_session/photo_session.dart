import 'package:flutter/material.dart';

class PhotoSession extends StatelessWidget {
  const PhotoSession({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Column(
        children: [
          Text('camera')
        ],
      ),
    );
  }
}
